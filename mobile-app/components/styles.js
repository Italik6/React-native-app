  'use strict';
  
  let React = require('react-native');
  
  let {
    StyleSheet,
  } = React;
  
  module.exports = StyleSheet.create({
    container: {
        backgroundColor: '#212121',
        height: '100%'
    },
    primaryText: {
        color: '#fff',
        fontSize: 20
    }
}); 